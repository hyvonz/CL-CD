# 项目文件逐行说明

本文档用于课堂展示和答辩说明，按文件解释项目中每一行配置或代码的作用。

## app/main.py

第 1 行：从 Flask 框架导入 `Flask` 和 `jsonify`，前者用于创建 Web 应用，后者用于返回 JSON 数据。
第 2 行：导入 Python 标准库 `os`，用于读取环境变量。
第 3 行：空行，用于分隔导入语句和应用初始化代码。
第 4 行：创建 Flask 应用对象，`__name__` 表示当前模块名。
第 5 行：空行，用于分隔应用对象和配置变量。
第 6 行：注释，说明下面代码用于读取环境配置。
第 7 行：读取 `APP_ENV` 环境变量；如果没有设置，则默认使用 `development`。
第 8 行：定义应用版本号，课堂演示时可修改这里触发一次新发布。
第 9 行：空行，用于分隔配置变量和路由定义。
第 10 行：注册首页路由 `/`。
第 11 行：定义首页处理函数 `home`。
第 12 行：返回一个多行 HTML 字符串。
第 13 行：页面主标题，显示项目名称。
第 14 行：显示应用运行状态。
第 15 行：显示当前应用版本号。
第 16 行：显示当前运行环境，并用红色加粗突出。
第 17 行：添加 HTML 分隔线。
第 18 行：显示应用由 Jenkins 和 Docker 管理。
第 19 行：结束多行 HTML 字符串。
第 20 行：空行，用于分隔两个路由。
第 21 行：注册健康检查接口 `/health`。
第 22 行：定义健康检查处理函数 `health`。
第 23 行：注释，说明该接口用于检查服务健康状态。
第 24 行：返回 JSON 响应和 HTTP 200 状态码，表示服务正常。
第 25 行：空行，用于分隔两个路由。
第 26 行：注册版本接口 `/version`。
第 27 行：定义版本接口处理函数 `version`。
第 28 行：注释，说明该接口用于查看版本。
第 29 行：返回包含版本号的 JSON 响应和 HTTP 200 状态码。
第 30 行：空行，用于分隔路由和启动入口。
第 31 行：判断当前文件是否作为主程序直接运行。
第 32 行：注释，说明监听 `0.0.0.0` 是为了让容器外部可以访问应用。
第 33 行：启动 Flask 服务，监听所有网卡的 5000 端口。

## app/requirements.txt

第 1 行：声明 Flask 版本为 `3.0.3`，用于运行 Web 应用。
第 2 行：声明 Pytest 版本为 `8.2.1`，用于执行自动化测试。

## test/test_health.py

第 1 行：导入 Pytest 测试框架。
第 2 行：导入 `sys`，用于修改 Python 模块搜索路径。
第 3 行：导入 `os`，用于处理文件路径。
第 4 行：空行，用于分隔导入语句和路径处理。
第 5 行：把项目根目录加入 Python 搜索路径，保证测试文件可以导入 `app.main`。
第 6 行：从应用代码中导入 Flask 应用对象 `app`。
第 7 行：空行，用于分隔导入和测试夹具。
第 8 行：使用 `pytest.fixture` 定义测试夹具。
第 9 行：定义名为 `client` 的测试客户端函数。
第 10 行：中文文档字符串，说明该函数用于配置 Flask 测试客户端。
第 11 行：开启 Flask 测试模式。
第 12 行：创建 Flask 测试客户端，并在上下文中使用。
第 13 行：把测试客户端交给具体测试函数使用。
第 14 行：空行，用于分隔测试夹具和测试函数。
第 15 行：定义 `/health` 接口测试函数。
第 16 行：中文文档字符串，说明该测试检查状态码和 JSON 内容。
第 17 行：使用测试客户端请求 `/health`。
第 18 行：断言接口返回 HTTP 200。
第 19 行：把响应内容解析成 JSON。
第 20 行：断言 JSON 中的 `status` 字段为 `ok`。
第 21 行：空行，用于分隔测试函数。
第 22 行：定义 `/version` 接口测试函数。
第 23 行：中文文档字符串，说明该测试检查版本接口。
第 24 行：请求 `/version`。
第 25 行：断言接口返回 HTTP 200。
第 26 行：断言 JSON 响应中包含 `version` 字段。
第 27 行：空行，用于分隔测试函数。
第 28 行：定义首页测试函数。
第 29 行：中文文档字符串，说明该测试检查首页是否能打开。
第 30 行：请求首页 `/`。
第 31 行：断言首页返回 HTTP 200。
第 32 行：断言首页 HTML 中包含项目标题文本。

## Dockerfile

第 1 行：使用官方 `python:3.11-slim` 作为基础镜像，提供 Python 运行环境。
第 2 行：空行，用于分隔基础镜像和工作目录配置。
第 3 行：设置容器内工作目录为 `/app`。
第 4 行：空行，用于分隔目录配置和依赖复制。
第 5 行：先复制依赖文件到镜像中，便于 Docker 利用构建缓存。
第 6 行：安装 Python 依赖，`--no-cache-dir` 用于减少镜像体积。
第 7 行：空行，用于分隔依赖安装和源码复制。
第 8 行：复制 `app/` 目录下的应用源码到镜像内 `/app/app/`。
第 9 行：空行，用于分隔源码复制和环境变量。
第 10 行：设置默认运行环境为 `container`，可被 Compose 覆盖。
第 11 行：设置 Flask 应用入口模块为 `app.main`。
第 12 行：空行，用于分隔环境变量和端口声明。
第 13 行：声明容器内部服务端口为 5000。
第 14 行：空行，用于分隔端口声明和启动命令。
第 15 行：容器启动时执行 `python -m app.main`，运行 Flask 应用。

## docker-compose.test.yml

第 1 行：声明 Compose 的服务列表。
第 2 行：定义测试环境服务名 `cicd-demo-test`。
第 3 行：指定使用 `cicd-demo` 镜像，标签来自 `IMAGE_TAG`，未设置时默认 `latest`。
第 4 行：指定容器名称为 `cicd-demo-test`，便于展示和排查。
第 5 行：开始定义容器环境变量。
第 6 行：把 `APP_ENV` 设置为 `Testing`，页面和健康检查会显示测试环境。
第 7 行：开始定义端口映射。
第 8 行：把宿主机 18080 端口映射到容器 5000 端口。
第 9 行：设置容器异常停止后自动重启，手动停止时不自动重启。

## docker-compose.prod.yml

第 1 行：声明 Compose 的服务列表。
第 2 行：定义生产环境服务名 `cicd-demo-prod`。
第 3 行：指定使用 `cicd-demo` 镜像，标签来自 `IMAGE_TAG`，未设置时默认 `latest`。
第 4 行：指定容器名称为 `cicd-demo-prod`，便于和测试环境区分。
第 5 行：开始定义容器环境变量。
第 6 行：把 `APP_ENV` 设置为 `Production`，页面和健康检查会显示生产环境。
第 7 行：开始定义端口映射。
第 8 行：把宿主机 28080 端口映射到容器 5000 端口。
第 9 行：设置容器异常停止后自动重启，手动停止时不自动重启。

## Jenkinsfile

第 1 行：声明 Jenkins Declarative Pipeline 开始。
第 2 行：使用任意可用 Jenkins Agent 执行流水线。
第 3 行：空行，用于分隔 Agent 和环境变量。
第 4 行：开始定义流水线环境变量。
第 5 行：定义镜像名称为 `cicd-demo`。
第 6 行：使用 Jenkins 构建编号作为镜像标签，实现每次构建版本可区分。
第 7 行：定义测试环境健康检查地址。
第 8 行：定义生产环境健康检查地址。
第 9 行：结束环境变量定义。
第 10 行：空行，用于分隔环境变量和阶段定义。
第 11 行：开始定义流水线阶段集合。
第 12 行：定义 `Checkout` 阶段。
第 13 行：开始定义该阶段的执行步骤。
第 14 行：从 Jenkins 任务绑定的 Git 仓库拉取代码。
第 15 行：结束步骤定义。
第 16 行：结束 `Checkout` 阶段。
第 17 行：空行，用于分隔阶段。
第 18 行：定义 `Unit Test` 阶段。
第 19 行：开始定义测试阶段步骤。
第 20 行：安装项目依赖。
第 21 行：执行 `test/` 目录下的 Pytest 测试。
第 22 行：结束步骤定义。
第 23 行：结束 `Unit Test` 阶段；如果测试失败，后续阶段不会继续。
第 24 行：空行，用于分隔阶段。
第 25 行：定义 `Docker Build` 阶段。
第 26 行：开始定义镜像构建步骤。
第 27 行：构建 Docker 镜像，同时打构建编号标签和 `latest` 标签。
第 28 行：结束步骤定义。
第 29 行：结束 `Docker Build` 阶段。
第 30 行：空行，用于分隔阶段。
第 31 行：定义 `Deploy to Test` 阶段。
第 32 行：开始定义测试环境部署步骤。
第 33 行：把镜像标签传给 Compose，并启动测试环境容器。
第 34 行：结束步骤定义。
第 35 行：结束 `Deploy to Test` 阶段。
第 36 行：空行，用于分隔阶段。
第 37 行：定义 `Smoke Test` 阶段。
第 38 行：开始定义冒烟测试步骤。
第 39 行：最多请求测试环境健康检查 5 次，`--noproxy "*"` 用于绕过本地代理，成功则通过，全部失败则返回失败。
第 40 行：结束步骤定义。
第 41 行：结束 `Smoke Test` 阶段。
第 42 行：空行，用于分隔阶段。
第 43 行：定义 `Deploy to Production` 阶段。
第 44 行：开始定义生产部署步骤。
第 45 行：加入人工确认，模拟真实项目发布审批。
第 46 行：通过生产 Compose 文件启动生产环境容器。
第 47 行：最多请求生产环境健康检查 5 次，`--noproxy "*"` 用于绕过本地代理，验证生产部署是否成功。
第 48 行：结束步骤定义。
第 49 行：结束 `Deploy to Production` 阶段。
第 50 行：结束阶段集合。
第 51 行：空行，用于分隔阶段和后置操作。
第 52 行：开始定义后置操作。
第 53 行：声明无论流水线成功或失败都执行下面操作。
第 54 行：打印名称包含 `cicd-demo` 的 Docker 容器状态，方便查看部署结果。
第 55 行：结束 `always` 后置操作。
第 56 行：结束 `post` 块。
第 57 行：结束整个 Pipeline。

## .dockerignore

第 1 行：忽略 Git 仓库目录，避免把版本历史发送到 Docker 构建上下文。
第 2 行：忽略 Pytest 缓存目录，减少无关文件。
第 3 行：忽略 Python 字节码缓存目录。
第 4 行：忽略 Python 编译产生的 `.pyc` 文件。
第 5 行：忽略名为 `venv` 的本地虚拟环境。
第 6 行：忽略名为 `.venv` 的本地虚拟环境。

## README.md

第 1 行：项目标题。
第 3 行：说明项目目标是演示 Jenkins 与 Docker 的容器化 CI/CD。
第 5-13 行：说明项目目录结构和各文件职责。
第 15-22 行：说明项目使用的技术栈。
第 24-37 行：说明如何在本地安装依赖、启动应用和访问接口。
第 39-45 行：说明如何执行本地自动化测试。
第 47-57 行：说明如何构建 Docker 镜像和直接运行容器。
第 59-77 行：说明如何通过 Docker Compose 分别部署测试环境和生产环境。
第 79-90 行：说明 Jenkins 流水线的 6 个阶段。
第 92-101 行：说明课堂演示时的完整操作流程。
第 103-106 行：说明项目使用的两个关键环境变量。
